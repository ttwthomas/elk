# Default Kibana configuration for docker target
server.name: kibana
server.host: "0"
server.basePath: "/proxy/5601"
elasticsearch.hosts: [ "http://elasticsearch:9200" ]
monitoring.ui.container.elasticsearch.enabled: true
